name: learnly
description: Learnly - Online Course Platform | OS2 Project | Mansoura University

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.15.4
  cached_network_image: ^3.3.1
  crypto: ^3.0.3
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
